import styles from './Header.module.css'

export default function Header() {
	return (
			<header className={styles.header}>
				<div className={styles.title}>
					<span className={styles.first}>ka</span>
					<span className={styles.second}>ro</span>
				</div>
			</header>
	)
}
